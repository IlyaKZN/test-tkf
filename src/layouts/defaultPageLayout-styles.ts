import styled from 'styled-components';

const Background = styled.main`
  width: 100%;
  height: calc(100vh - 120px);
  background-color: white;
`;

const ContentContainer = styled.section`
  margin: 0 auto;
  padding: 80px 0;
  max-width: 1140px;
`;

export { Background, ContentContainer };
