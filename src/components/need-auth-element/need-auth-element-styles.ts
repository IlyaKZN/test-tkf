import { Link } from 'react-router-dom';
import styled from 'styled-components';

const Title = styled.h1`
  font-family: 'Alegreya Sans';
  font-size: 40px;
  text-align: center;
`;

const LoginLink = styled(Link)`
  font-family: 'Alegreya Sans';
  font-size: 40px;
`;

export { Title, LoginLink };
